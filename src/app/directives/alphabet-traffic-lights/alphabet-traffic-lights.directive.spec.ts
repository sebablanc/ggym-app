import { AlphabetTrafficLightsDirective } from './alphabet-traffic-lights.directive';

describe('AlphabetTrafficLightsDirective', () => {
  it('should create an instance', () => {
    const directive = new AlphabetTrafficLightsDirective();
    expect(directive).toBeTruthy();
  });
});
